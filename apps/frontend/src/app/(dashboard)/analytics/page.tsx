import { PageWrapper } from "@/components/layout/PageWrapper";
import { Card } from "@/components/ui/Card";

export default function AnalyticsPage(): JSX.Element {
  return (
    <PageWrapper title="Web Analytics">
      <Card>
        <p className="text-text2">Donut sources, carte monde SVG, top pages et KPIs secondaires à implémenter.</p>
      </Card>
    </PageWrapper>
  );
}
